# Container Detail

Detailed view of a specific container's configuration and runtime metrics.

![Screenshot of Container Detail page - Screenshot of the Container Detail page showing environment variables, network bindings, and live logs.](/docs/wiki/screenshots/11.container-detail.png)

## Page Details
Provides deep inspection of a container. Shows environment variables, mounted volumes, network ports, and real-time logs streamed via WebSocket.

## Dialogs
Execute Command (Exec) dialog: Gathers the command to run inside the container and displays the interactive terminal.
