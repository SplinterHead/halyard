# Nodes

List of all physical or virtual machines participating in the Docker Swarm.

![Screenshot of Nodes page - Screenshot of the Nodes view showing a table of cluster nodes with their roles, availability, and status.](/docs/wiki/screens/08-nodes.png)

## Status Indicators
- **Active** (Green): Indicates the item is active.
- **Inactive** (Grey): Indicates the item is inactive.

## Row Actions
- **Clicking a row**: Navigates to the details page for the selected item.

## Buttons
- **Search Input**: Filters the list of items based on the input query.
- **Add Node**: Opens a dialog or navigates to create a new item.
- **Refresh**: Reloads the current data from the manager.
- **{{ copiedWorker ? 'Copied!' : 'Copy Command' }}**: Triggers the {{ copiedworker ? 'copied!' : 'copy command' }} action.
- **{{ copiedManager ? 'Copied!' : 'Copy Command' }}**: Triggers the {{ copiedmanager ? 'copied!' : 'copy command' }} action.

## Dialogs
- **mdi-docker           Add Node to Swarm Cluster**: Retrieving Swarm Join Tokens...                                    mdi-alert-circle-outline             Failed to fetch tokens             [Item]                                                   To expand your Docker Swarm cluster, execute the appropriate command below directly inside the terminal of the node you wish to join.                                                                        Join as Worker (Recommended)                                    [Item]                                                                [Item]                                                                                       Join as Manager                                    [Item]                                                                [Item]                                                                      Note: Make sure port 2377/tcp (Swarm clustering), 7946/tcp/udp (Node communication), and 4789/udp (Overlay network) are open on your firewalls.
