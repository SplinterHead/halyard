# Networks

List of Docker networks (overlay, bridge, etc.) configured in the Swarm.

![Screenshot of Networks page - Screenshot of the Networks view showing network names, drivers, and scopes.](/docs/wiki/screenshots/14.networks.png)

## Page Details
Displays a table of all networks. Differentiates between 'swarm' (overlay) scoped networks and 'local' scoped networks.

## Dialogs
Create Network dialog (gathers network name, driver type, and subnet configurations). Delete confirmation dialog.
