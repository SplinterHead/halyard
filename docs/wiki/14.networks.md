# Networks

List of Docker networks (overlay, bridge, etc.) configured in the Swarm.

![Screenshot of Networks page - Screenshot of the Networks view showing network names, drivers, and scopes.](/docs/wiki/screens/14-networks.png)

## Status Indicators
- **Active** (Green): Indicates the item is active.
- **Inactive** (Grey): Indicates the item is inactive.

## Row Actions
- **Clicking a row**: Navigates to the details page for the selected item.

## Buttons
- **Search Input**: Filters the list of items based on the input query.
- **Create Network**: Opens a dialog or navigates to create a new item.
- **Refresh**: Reloads the current data from the manager.
- **Delete Outline**: Prompts a confirmation dialog to delete the selected item.
- **Create**: Opens a dialog or navigates to create a new item.
- **Remove Network**: Prompts a confirmation dialog to delete the selected item.
- **Dismiss**: Triggers the dismiss action.

## Dialogs
- **Create Swarm Network**: !!v || 'Name is required']"               required               class="mb-4"             >                                                                                                                                                                  IPAM Configuration (Optional)
- **Delete Network?**: Are you sure you want to remove the network [Item]? This action cannot be undone.
- **mdi-alert-circle           Network Deletion Failed**: The network could not be deleted. This usually happens if it is still being used by one or more services or containers.                        [Item]
