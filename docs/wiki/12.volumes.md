# Volumes

List of all storage volumes present across the cluster nodes.

![Screenshot of Volumes page - Screenshot of the Volumes view showing a table of volumes with their names, drivers, and mount points.](/docs/wiki/screens/12-volumes.png)

## Status Indicators
- **local** (Grey): Indicates the item is local.
- **nfs** (Blue): Indicates the item is nfs.
- **gluster** (Grey): Indicates the item is gluster.
- **local** (Grey): Indicates the item is local.

## Row Actions
- **Clicking a row**: No default action. Use the action buttons for interactions.

## Buttons
- **Search Input**: Filters the list of items based on the input query.
- **Prune Unused**: Triggers the prune unused action.
- **Refresh**: Reloads the current data from the manager.
- **Prune Volumes**: Triggers the prune volumes action.
- **Remove Volume**: Prompts a confirmation dialog to delete the selected item.
- **Dismiss**: Triggers the dismiss action.
- **Browse Volume Files**: Triggers the browse volume files action.
- **Delete Volume**: Prompts a confirmation dialog to delete the selected item.

## Dialogs
- **Prune Unused Volumes?**: This will remove ALL volumes on ALL nodes that are not currently used by at least one container.           This action cannot be undone.
- **mdi-alert-decagram           Delete Volume?**: Are you sure you want to remove the volume [Item] on node [Item]? This action cannot be undone.
- **mdi-alert-circle           Volume Deletion Failed**: The volume could not be deleted. This usually happens if it is still being used by one or more containers on the node.                        [Item]
