# Volumes

List of all storage volumes present across the cluster nodes.

![Screenshot of Volumes page - Screenshot of the Volumes view showing a table of volumes with their names, drivers, and mount points.](/docs/wiki/screenshots/12.volumes.png)

## Page Details
Displays a data table of volumes. Includes functionality to search and filter. Features a global 'Prune Unused' button.

## Dialogs
- **Prune Confirmation Dialog**: Warns the user that all unused volumes across all nodes will be permanently removed. 
- **Delete Confirmation Dialog**: Asks for confirmation before removing a specific volume. 
- **Error Dialog**: Displays a failure message if a volume cannot be deleted (e.g., if it's currently in use).
